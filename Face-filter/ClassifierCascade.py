from const_libraries import *

class ClassifierCascade:
    def __init__(self, count, is_stump_based, has_tilted_features, is_tree, inv_window_area, sum, sqsum, tilted, stage_classifier, sqsumtype, sumtype, stages):

    #detect_multi_scale()
